#include <stdio.h>
int main(){

   
int x=0,y=0,z=0,w=0,result=0;

printf("enter first number to be calculated by C:");
scanf("%d",&x);
printf("enter second number to be calculated by C:");
scanf("%d",&y);
printf("enter third number to be calculated by C:");
scanf("%d",&z);
printf("enter fourth number to be calculated by C:");
scanf("%d",&w);





result=x*y*z*w;
printf("product of 4 numbers:%d",result);



return 0;
}