// Creating custom modules

console.log("This is Module")

function avgr(arr) {
    let sum=0;
    arr.forEach(element => {
        sum+=Element;
    });
    return sum/arr.length;
}

module.exports = avgr;

module.exports ={
    average:avgr,
    name:"Ritesh",
    repo:"GIT HUB"
} 
    
